{
  "name": "Dehumidifier Timer",
  "attributes": {
    "minutes": {
      "schema": {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "minimum": 0,
            "maximum": 720
          },
          "unit": {
            "type": "string",
            "enum": [
              "min"
            ],
            "default": "min"
          }
        },
        "additionalProperties": false,
        "required": [
          "value"
        ]
      },
      "setter": "setTimer",
      "enumCommands": []
    }
  },
  "commands": {
    "setTimer": {
      "name": "setTimer",
      "arguments": [
        {
          "name": "minutes",
          "optional": false,
          "schema": {
            "type": "integer",
            "minimum": 0,
            "maximum": 720
          }
        }
      ]
    }
  }
}
