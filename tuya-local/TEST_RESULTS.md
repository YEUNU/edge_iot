# 검증 기록 — 2026-09-16

## 자동 검증

- Tuya Python 29개 테스트 통과. 저장소 전체 Python 48개 테스트와 별도로 추가한 개인정보 검사 3개 테스트도 통과.
- Lua HTTP 전송 계층 18개 시나리오 통과.
- Edge 화면 수명주기, 자동 등록·재시작, 기종 탐색·시험·취소, 장애 복구, 명령 대기열, 비동기 프로필 적용 회귀 검사 통과.
- 313개 카탈로그의 74,743개 명령 전체를 모의 Tuya 장치로 재생하여 저장된 원본 payload와 일치함을 확인.
- Python 컴파일, Lua 문법, SmartThings 드라이버 패키징 통과.
- 드라이버 ZIP에 실제 local key, 브리지 bearer token, runtime data/state가 포함되지 않음.

## 추가 검사에서 수정한 문제

- 연결 실패 시 온라인 상태가 남던 문제: 실패 시 오프라인, 이후 정상 조회 시 복구.
- HTTP 200 응답의 JSON이 객체가 아닐 때 처리 누락: 비정상 응답으로 거부.
- 비동기 프로필 변경 중 설정 값이 유실되던 문제: 프로필 적용 후 조회에서 다시 발행.
- 재시작 후 이전 온도·풍량·모드 소실: 기종별로 원자적 저장·복원. 실제 전원 상태는 추정하지 않음.
- 유휴 Tuya 소켓의 일시적 실패: IR을 보내지 않는 상태 조회만 연결을 열어 한 번 재시도. 응답이 불확실한 IR 명령은 재전송하지 않음.

## 실제 설치에서 확인

- 기기/브리지 주소를 인자 없이 찾는 자동 연결 스크립트가 성공하고 허브가 일회용 티켓을 소비함.
- 브리지 재생성과 Edge 드라이버 업데이트 후 수동 토큰 입력 없이 연결됨.
- 실제 SmartThings 명령에서 LAN 브리지와 Tuya 허브 응답까지 확인.
- 실제 SmartThings 웹에서 기본 조작 화면, 현재 설정 값, Carrier의 18–30°C 범위 확인.
- 최종 SmartThings health: ONLINE. 기종은 기존 Carrier로 유지.

## 아직 검증하지 않은 것

- 312개 다른 공개 코드셋의 실제 에어컨 호환성은 검증하지 않음. 전체 재생 검사는 변환된 데이터의 경로와 일치성을 확인한 것임.
- 장시간 운용, 공유기 재부팅, DHCP 주소 변경의 실물 시나리오는 확인하지 않음. 주소 변경 시 자동 연결을 다시 실행해야 함.

## 모바일·실물 최종 검증

2026-09-16 21:53–21:59 KST, 사용자가 허용한 ADB 연결로 실제 Galaxy SM-S938N의 SmartThings 앱을 확인했다.

- 내부 영문 capability 제목을 한글로 번역하고, 상단의 큰 연결 상태 카드를 제거했다. 전원 다음에 설정 온도·모드·풍량, 아래에 기종 설정이 표시되는 것을 실제 화면에서 확인했다.
- Carrier의 7개 기종 후보 목록, 현재 코드셋 선택과 저장 버튼의 정상 처리를 확인했다.
- 기종 설정/기본 화면의 프로필 전환 시 앱이 재진입을 요구하는 제약을 확인하고 README에 기록했다.
- 앱의 온도 슬라이더로 냉방 26°C, 풍량 목록으로 보통을 선택한 뒤 전원을 껐다. 화면과 LAN 상태 조회에서 냉방·26°C·medium, 최종 power=false를 확인했다.
- 사용자가 “켜짐·설정 변경·꺼짐 모두 반응함”으로 실물 동작을 확인했다. 이는 현재 Carrier에 대한 검증이며 다른 312개 기종의 물리 호환성 검증을 뜻하지 않는다.
- 자동 연결과 기존 카탈로그를 유지한 채 변경된 UI를 실제 허브에 배포했다.

재실행 방법은 README의 검증 절에 기록했다. 테스트는 실제 IR을 반복 송신하지 않는다.
## 로컬 전용 통신과 모드 드롭다운

- Python 29개 테스트 통과. 외부 연결·DNS 차단, 허브 목적지 허용 및 HTTP 수신 유지 검사 추가.
- 컨테이너에서 `/data`에 `config.json`만 존재하고 클라우드 수집 스크립트가 없음을 확인. 새 이미지에서 실제 허브 상태 조회 및 자동 연결 티켓 교환 성공.
- 실제 Galaxy 앱의 일상 화면에서 모드 드롭다운 표시를 확인. 송풍을 선택한 뒤 LAN 브리지 상태가 송풍/25°C/자동 풍량으로 변경됐고, 냉방으로 복귀한 뒤 앱 전원 버튼으로 끔을 확인.
- 기종 시험에서 Carrier 1161을 선택만 하고 IR을 보내지 않음. 앱 드롭다운에 냉방·제습·송풍만 나타나며 자동·난방이 제외됨을 XML로 검증.
- Lua 테스트에 화면용 모드 명령의 후보/저장 기종 분리 검사를 추가하고 통과.
- Tuya 허브용 제조사 729개와 중복 제외 리모컨 인덱스 1,325개 목록 및 rules 조회 완료. 코드 레코드 163,693개를 내려받았고 37개 인덱스는 비어 있지 않은 코드를 반환하지 않음. 이 숫자는 사용 가능한 로컬 코드셋 수가 아님. 추가 코드의 로컬 송신 형식 변환·검증은 미완료.
- 후보 시험 화면의 변경 취소 버튼을 눌러 기존 Carrier 프로필과 냉방 설정으로 복귀함을 확인. 최종 전원 끄기 명령 성공. 시험 후보에 실제 IR은 전송하지 않음.

## 코드 정리와 공개 전 점검

- 사용하지 않는 연결 capability와 이전 켜짐·꺼짐 시험 명령을 제거했다. 기종 시험은 전원·온도·모드·풍량 조작을 사용한다.
- 기존 Lua 42개 검사, Tuya 전송 계층 18개 검사 및 UI 흐름 회귀 검사를 통과했다.
- 공개 대상 전체와 gzip 카탈로그에서 실제 설정의 비밀값을 검사했고 발견되지 않았다. Gitleaks의 공개 파일 및 기존 Git 이력 검사에서도 비밀값이 발견되지 않았다.
- 개인 설정·상태·수집 원본·로그 등을 Git에서 제외하고, 강제 추가된 개인정보 파일까지 확인하는 검사기를 추가했다.
- 기존 커밋의 작성자 이메일은 사용자의 선택에 따라 유지한다. 새 커밋에는 GitHub noreply 주소를 사용한다.
- 최종 모바일 검증 상태는 기존 Carrier, 냉방 25°C·자동 풍량·전원 꺼짐이다. 다른 에어컨의 실물 호환성 및 추가 1,325개 인덱스의 로컬 변환은 완료되지 않았다.
